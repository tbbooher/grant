= simple_form_for @prayer_request, :html => { :class => 'form-horizontal' } do |f|
  = f.input :_id
  = f.input :request
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), prayer_requests_path, :class => 'btn'
